{
  "name": "mo-convert",
  "version": "0.0.1",
  "license": "MIT",
  "scripts": {
    "generate": "git submodule update && node ./generate && npm run format:mo",
    "format:mo": "prettier --write \"src/**/*.{mo,json}\""
  },
  "devDependencies": {
    "prettier": "^3.0.2",
    "prettier-plugin-motoko": "^0.8.1"
  }
}
